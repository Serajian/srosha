Branch: `master`

# Summary

دو فایلی که `CLAUDE.md` به‌عنوان قانون STRICT به آن‌ها ارجاع می‌داد ولی وجود
نداشتند، ساخته شدند.

`docs/ARCHITECTURE.md` عمداً خالی است. فقط یک عنوان دارد و یک توضیح: محتوایش
تدریجی و از روی چیزی که واقعاً تصمیم گرفته و ساخته می‌شود نوشته می‌شود، نه از روی
یک نقشهٔ از پیش آماده، و هر ورودی قبل از نوشته‌شدن با مالک چک می‌شود. تا وقتی خالی
است چیزی را محدود نمی‌کند و همین وضعیت صادقانه است.

`docs/CONFIG.md` پر شد. هفت بخش دارد: Git، Deployment، Services and ports،
Application configuration، Migrations، Local development، Build targets. منبع
داده‌هایش `docs/reference/srosha-infra-deploy.md` است، یعنی چیزی که واقعاً روی
سرور بالاست.

سه تصمیم موقع نوشتن `CONFIG.md`:

هیچ رمزی داخلش نرفت، حتی رمز محیط لوکال. نام نقش و نام دیتابیس داده‌اند و نوشته
شدند؛ جای رمز نوشته شد «در `.env` و در Environment tab دوکپلوی». فایل با grep
بررسی شد و هیچ credential ای در آن نیست.

کلیدهای env دوباره کپی نشدند. به‌جای تکرار تک‌تکشان یک جدول نوشته شد که می‌گوید هر
گروه مال کدام باینری است و برای جزئیات به `.env.example` ارجاع می‌دهد. اگر هر دو
جا نوشته شوند، یکی‌شان بالاخره کهنه می‌شود.

قانون رمز (فقط حرف و رقم و خط تیره، و سه لایه‌ای که با کاراکتر خاص می‌شکنند) داخل
`CONFIG.md` آمد، چون خودش داده است نه convention — یعنی «چه کاراکترهایی در این
محیط مجازند».

# Files Changed

- `docs/ARCHITECTURE.md` *(ساخته شد — عمداً خالی)*
- `docs/CONFIG.md` *(ساخته شد و پر شد)*

# Tests Run

هیچ کد Go تغییر نکرد. فایل `CONFIG.md` برای اطمینان از نبود credential با grep
بررسی شد — تمیز بود.

# Follow-ups / Risks

- `docs/ARCHITECTURE.md` خالی است، پس قانونی که می‌گوید «کد مخالف آن غلط است» فعلاً
  چیزی را اجرا نمی‌کند.
- `deployment/` هنوز کاملاً خالی است، پس مسیرهای compose و Dockerfile که در
  `CONFIG.md` ثبت شده‌اند به فایل‌های موجودنداشته اشاره می‌کنند.

# Instruction

قرار بر این شد که مالک این دو فایل را از قبل ننویسد؛ من حین کار پیش‌نویسشان را
جمع کنم و **قبل از ثبت، دقیقاً همان متن را نشانش بدهم و تأیید بگیرم**. ساختن
اسکلت هر دو فایل تأیید شد، و بعد محتوای `CONFIG.md` به‌صورت یک جدول پیشنهاد و
تأیید شد.
