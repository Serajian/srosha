Branch: `<type>/<slug>`

# Summary

What changed and why, in prose. Name the actual symbols and packages, not generalities —
a reader who was not here must be able to tell what moved without opening the diff.

# Files Changed

- `path/to/file` *(short note on what happened to it)*

# Tests Run

- `go build ./...` — clean
- `golangci-lint run` — clean
- `go test -race ./...` — pass

# Follow-ups / Risks

- Anything left undone, anything that could bite later, or "None." with the evidence for why.

# Instruction

What the user asked for, written as the **understanding that was confirmed before the work
started** — not the raw message. "Do it" records nothing; what was agreed does. If the task
took shape over several messages, record the agreed intent as it ended up.
