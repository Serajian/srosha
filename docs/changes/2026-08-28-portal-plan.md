Branch: `feat/portal-spec`

# Summary

**planِ فازِ ۱، از روی spec ای که تأیید شد.** هیچ کدی عوض نشد.

# دامنه کوچک‌تر از فازِ ۱ است، و عمداً

spec چهار قدم دارد. این plan دو تای اولش را می‌گیرد:

```
این plan     باینری · کاربر · ورود · session · گلوگاه · حسابرسی
plan ــِ دوم  ثبتِ source · کلید · credential · webhook
```

چون قدمِ اول و دوم خودشان یک چیزِ **کامل و آزمودنی** اند — «کاربر وارد می‌شود،
غیرفعال نمی‌تواند، و هر تغییری ثبت می‌شود» — و همه‌چیزِ بعدی رویشان سوار است.
یکی کردنشان یک planِ غول می‌ساخت که هیچ‌کس تا آخرش نمی‌رسید.

# ده task، و هرکدام سرِ جایش می‌ایستد

هر task با «`make prepush`، بعد بایست» تمام می‌شود. **هیچ‌کدام commit نمی‌کند** —
قاعدهٔ خودِ مخزن است و در plan نوشته شد تا کسی که آن را اجرا می‌کند نداند و
نزند.

# بازبینیِ خودم، دو ایراد

## ۱ — Task 9 دو تا `t.Skip` داشت

که دقیقاً همان placeholder ای است که این skill ممنوع کرده: «تست را بعداً
بنویس». نوشته بودم «برو فایلِ دیگری را بخوان و از روی آن بنویس» — که برای کسی که
فقط task ــِ خودش را می‌بیند، هیچ است.

موقعِ نوشتنِ تستِ واقعی، دو چیز معلوم شد:

```
smtp.Message.To      رشته است، نه []string   ← در plan غلط نوشته بودم
smtp.Message.Body    نه Text                  ← همان
stand-in ــِ موجود   nil برمی‌گرداند          ← پس send اصلاً آزمودنی نیست
```

پس ساختنِ پیام یک تابعِ **خالص** شد. و از دلِ همین یک تستِ تازه درآمد که در
طراحی به آن فکر نکرده بودم:

> **کد نباید در subject باشد** — subject روی صفحهٔ قفلِ گوشی نشان داده می‌شود،
> و کلِ نکتهٔ کد این است که داشتنِ صندوقِ ایمیل چیزی را ثابت کند.

## ۲ — `Gate` هیچ صداکننده‌ای نداشت

plan آن را می‌سازد و هیچ task ای صدایش نمی‌زند. بوی کدِ مرده می‌دهد.

spec دلیلش را دارد — گلوگاه **قبل از** اولین نویسنده ساخته می‌شود، وگرنه
گلوگاهی است با ده راهِ دور زدن — ولی plan این را نگفته بود. حالا می‌گوید، و یک
تستِ integration اضافه شد که `Gate` را با repository ــِ واقعی کنار هم می‌گذارد،
تا تا planِ دوم بی‌آزمون نماند.

# و یک تصمیمِ طراحی که در نوشتنِ تست گرفته شد

Task 5 تستی دارد که **در برابرِ پیاده‌سازیِ خودِ همان task شکست می‌خورد**، و این
عمدی است. سؤال این است: وقتی تغییری شکست می‌خورد، حسابرسی سطری دارد یا نه؟

```
اول ثبت کن     تلاشِ شکست‌خورده هم سطر دارد     ← آنچه یک بررسی لازم دارد
بعد ثبت کن     تغییری که ثبت نشد، انجام شد     ← بدتر
```

plan جوابش را می‌دهد (اول ثبت) و می‌گوید تست را با آن هم‌راستا کن — به‌جای اینکه
تصمیم را قایم کند.

# Files Changed

- `docs/superpowers/plans/2026-08-28-portal-foundation.md` *(تازه)*

# Tests Run

هیچ کدی عوض نشد. `make prepush` — سبز.

# Follow-ups / Risks

- **plan چند جا می‌گوید «اول این فایل را بخوان».** برای `smtp.Client.Send`،
  برای شکلِ `settings.SMTP`، و برای stand-in ــِ `Dialer`. این‌ها placeholder
  نیستند — کدِ واقعی را داده‌ام — ولی یعنی مجری باید تأییدشان کند و کورکورانه
  کپی نکند.
- **Task 10 بزرگ‌ترین است** و صفحه و قالب و پیکربندی و باینری را با هم دارد.
  اگر موقعِ اجرا سنگین شد، شکستنش به «باینری که بالا می‌آید» و «صفحه‌ها» کارِ
  درستی است.
- **`sources.owner_user_id` و `may_use_shared_sender` در این plan نیستند.** با
  ثبتِ source می‌آیند، و آن لبهٔ `Registry.For` که spec اسمش را برد و تصمیمش را
  نگرفت، همان‌جا باید گرفته شود.

# Instruction

«تأیید» — روی spec، که طبق فرایند یعنی نوشتنِ implementation plan.
