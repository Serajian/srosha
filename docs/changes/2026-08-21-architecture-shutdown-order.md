Branch: `feat/bootstrap`

# Summary

تصمیم tier ها در `docs/ARCHITECTURE.md` ثبت شد. کد این تصمیم قبلاً در `cf5301c`
نوشته شده بود؛ این فقط خودِ تصمیم و دلیلش را جایی می‌گذارد که binding است.

ورودی سه چیز را می‌گوید:

- ترتیب: از بیرون به داخل — listener ها، بعد client های خروجی، بعد broker، بعد
  store. tier ها در `internal/registry/const.go` نام دارند و هر کدام دلیلش را
  کنار خودش.
- راه دیگری که انتخاب **نشد**: معکوسِ ترتیب باز شدن. کار می‌کند و خودش را نگه
  می‌دارد، ولی چیزی نمی‌گوید — ترتیب می‌شود نتیجهٔ اینکه `bootstrap` اتفاقاً چطور
  نوشته شده، و جابه‌جا شدن دو خط بی‌صدا عوضش می‌کند.
- هزینه‌ای که آگاهانه پذیرفته شد: tier یک منبع حقیقت دوم است و tier اشتباه یک باگ
  خاموش که کامپایلر نمی‌گیردش.

یک بخش هم دربارهٔ readiness دارد: چرا ترتیبی برایش تعریف نشده (پرسیدن چیزی را از زیر
پای کسی نمی‌کشد)، و چرا هر dependency را جدا گزارش می‌دهد به‌جای یک خطای joined.

# Files Changed

- `docs/ARCHITECTURE.md` *(ورودی «Shutdown order is declared, not inherited»)*

# Tests Run

- تغییری در کد نبود.
- تصمیم در یک اجرای واقعی دیده شد: خاموشی dispatcher به ترتیب `http server` (۳)،
  `sender http client` (۲)، `webhook http client` (۲)، `nats` (۱)، `postgres` (۰).
  آن دو خط tier=2 همان شاخهٔ «داخل یک tier، معکوسِ ترتیب ساخته شدن» است.

# Follow-ups / Risks

- هیچ. کد و سند حالا یک چیز می‌گویند.

# Instruction

در همان گفتگویی که tier ها انتخاب شدند، قرار شد تصمیم در `ARCHITECTURE.md` هم ثبت
شود. متن قبل از نوشتن تأیید شد.
